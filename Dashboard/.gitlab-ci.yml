image: node:18

cache:
  paths:
    - node_modules/

before_script:
  - npm install

pages:
  script:
    - npm run build
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - develop